#include <stdio.h>

int main()
{
    int n,r;
    printf("enter a number\n");
    scanf("%d",&n);
    while(n!=0){
        r=r*10;
        r=r+n%10;
        n=n/10;
     
    }
printf("reverse:%d",r);
    return 0;
}

