#include <stdio.h>

int main()
{
   int n,r,x;
   printf("Enter a number\n");
   scanf("%d",&n);
   x=n;
   while(n!=0){
       r=r*10;
       r=r+n%10;
       n=n/10;
       
   }if(x==r)
   printf("number is a palendrome");
else
printf("number is not a palendrome");
    return 0;
}

