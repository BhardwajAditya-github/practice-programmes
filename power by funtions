#include <stdio.h>
int power(int x,int y);
int main()
{
    int a,b,result;
    scanf("%d%d",&a,&b);
    result=power(a,b);
    printf("%d",result);

    return 0;
}
int power(int x,int y){
    int p=1;
    while(y--){
    p=p*x;    
    }
    return p;
}
